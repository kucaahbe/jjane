Description:
    Creates JJane base files, takes 2 arguments: 'install' and 'update'

Example:
    `./script/generate jjane_base install` - this will install base jjane files

    `./script/generate jjane_base update` - this will update base jjane files(add new migration etc.)
